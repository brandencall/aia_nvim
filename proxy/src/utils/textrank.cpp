#include "textrank.h"

namespace utils {

std::string summarizeText(const std::vector<std::string> &text) {
    //Parse text into a vector of sentances. Strip down the markdown.
    std::string result;
    return result;
}


//Stripping markdown:
//  Code Block and inline code: (```[\s\S]*?```|~~~[\s\S]*?~~~|`[^`\n]*?`)

} // namespace utils
